import { FastifyRequest, FastifyReply } from "fastify";

import prisma from "../../lib/prisma";

/**
 * ========================================
 *        Display specific book
 * ========================================
 */
export const getOneBook = async (req: FastifyRequest, res: FastifyReply) => {};
